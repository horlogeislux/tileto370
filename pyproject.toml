[project]
name = "tileto500"
version = "1.0.0"
description = "Tile conversion from NetHack 3.6.x to 5.0.0."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pillow>=12.1.1",
]
license = "MIT"
license-files = ["LICENSE.txt"]


[tool.ruff.lint]
select = ["ALL"]
ignore = [
    "TD", # TODOs format
]
